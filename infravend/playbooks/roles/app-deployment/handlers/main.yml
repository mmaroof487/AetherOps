---
# Handlers for app deployment role

- name: reload systemd daemon
  systemd:
    daemon_reload: yes
  become: yes

- name: restart shopops service
  systemd:
    name: shopops
    state: restarted
  become: yes
